{...}: {
    hardware = {
        openrazer = {
            enable = true;
            users = [
                "hand7s"
            ];

            batteryNotifier = {
                enable = true;
                percentage = 15;
            };
        };
    };
}
